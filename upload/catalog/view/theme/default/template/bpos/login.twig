<!DOCTYPE html>
<html lang="en" data-theme="white">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Barik POS — Login</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="catalog/view/theme/default/stylesheet/bpos/login.css?v={{ random() }}">
   <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
</head>
<body>
  <div class="top-controls">
    <button class="ghost" id="themeToggle">Toggle Theme</button>
  </div>

  <main class="login-shell">
    <section class="brand-pane">
      <div class="logo"><div class="mark">B</div><div class="word">Barik <span>POS</span></div></div>
      <h1>{{ text_signin_desc }}</h1>
      <p>{{ text_login_desc }}</p>
      <div class="tabs" role="tablist" aria-label="Login method">
        <button class="tab" id="tab-password" role="tab" aria-selected="true" aria-controls="panel-password">Password</button>
        <button class="tab" id="tab-pin" role="tab" aria-selected="false" aria-controls="panel-pin">PIN & Keypad</button>
      </div>
    </section>

    <section class="card">
       {% if logo %}
            <div class="login-logo">
              <img src="{{ logo }}" alt="Logo" class="img-fluid mb-2">
            </div>
          {% endif %}
         
      <h2>{{ text_login }}</h2>
       {% if error_warning %}
            <div class="alert-login">{{ error_warning }}</div>
          {% endif %}
      <form action="{{ action }}" method="post" id="panel-password" role="tabpanel" aria-labelledby="tab-password">
        <div class="field">
          <label class="label" for="username">{{ text_username }}</label>
          <input class="input" id="username" name="username" type="text" autocomplete="username" required autofocus>
        </div>
        <div class="field">
          <label class="label" for="password">{{ text_password }}</label>
          <input class="input" id="password" name="password" type="password" autocomplete="current-password" required>
        </div>
        <div class="row">
          <label>
            <input type="checkbox" id="remember"> {{ text_remember_me }}
          </label>
          <!-- <a href="#" id="forgot">Forgot password?</a> -->
        </div>
        <button type="submit" class="btn">{{ text_signin }}</button>
      </form>

      <form id="panel-pin" action="{{ action }}" method="post" role="tabpanel" aria-labelledby="tab-pin" class="pin-wrap" autocomplete="off">
        <div class="field">
          <label class="label" for="username">Username</label>
          <input class="input" id="username" name="username" type="text" autocomplete="username" required autofocus>
        </div>
        <label class="label" for="pinInput">Enter your 6-digit PIN</label>
        <input id="pinInput" name="pin" type="password" inputmode="numeric" pattern="[0-9]{6}" maxlength="6" class="sr-only" aria-hidden="false">
        <div class="pin-input" aria-live="polite" aria-label="PIN dots">
          <div class="pin-dot" data-i="0"></div>
          <div class="pin-dot" data-i="1"></div>
          <div class="pin-dot" data-i="2"></div>
          <div class="pin-dot" data-i="3"></div>
          <div class="pin-dot" data-i="4"></div>
          <div class="pin-dot" data-i="5"></div>
        </div>
        <div class="keypad" aria-label="Numeric keypad">
          <button type="button" class="key" data-key="1">1</button>
          <button type="button" class="key" data-key="2">2</button>
          <button type="button" class="key" data-key="3">3</button>
          <button type="button" class="key" data-key="4">4</button>
          <button type="button" class="key" data-key="5">5</button>
          <button type="button" class="key" data-key="6">6</button>
          <button type="button" class="key" data-key="7">7</button>
          <button type="button" class="key" data-key="8">8</button>
          <button type="button" class="key" data-key="9">9</button>
          <button type="button" class="key fn secondary" data-action="clear" title="Clear">CLR</button>
          <button type="button" class="key" data-key="0">0</button>
          <button type="button" class="key fn" data-action="backspace" title="Backspace">⌫</button>
        </div>
        <div class="row">
          <button type="submit" class="btn" id="btnPinSign">{{ text_signin }}</button>
        </div>
      </form>

      <div class="statusbar" id="statusbar"><span>v1.0.0</span><span>•</span><span class="dot ok" id="statusDot"></span><span id="statusText">Online</span><span>•</span><span>© 2025 Barik</span></div>
    </section>
  </main>

   <script src="catalog/view/javascript/login-bpos.js?v={{ random() }}"></script>
</body>
</html>
